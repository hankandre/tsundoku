# Copy to .env (gitignored) for local development.
PUBLIC_API_URL=http://localhost:6060

# Must match apps/api/.env — SvelteKit verifies the access cookie locally
# rather than calling /auth/me on every request, so the secrets need to agree.
JWT_SECRET=replace-me-with-a-32-byte-hex-string-or-longer-secret
