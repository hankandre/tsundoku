> [!WARNING]
> This application is undergoing a rewrite from its original stack to a new one. DO NOT USE.

## Tsundoku

A fork and rewrite of Booklore.

## AI Usage

The goal is to use AI to completely rewrite the application, or at least make a rough go of it and then take on the code authoring from that point forward. This is done in the hopes of getting close to feature-parity with the original booklore app but with the new bun/Hono and SvelteKit stacks.
